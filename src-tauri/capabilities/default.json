{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Base permissions for the TA Character Studio window",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-confirm"]
}
